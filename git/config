# This is Git's per-user configuration file.
[user]
  name = Stel Abrego
  email = stel@stel.codes
[init]
  defaultBranch = "main"
[merge]
  ff="only"
  # Adds a third section to the conflict output
  conflictStyle = diff3
[push]
  autoSetupRemote = true
[pull]
  ff="only"
[core]
  excludesFile="~/.config/git/ignore"
  pager="delta --syntax-theme 'base16' --hunk-header-style 'file line-number syntax'"
[alias]
  fancy-log = "log --pretty=format:'%C(yellow)%h%C(reset) %C(blue)%an%C(reset) %C(cyan)%cr%C(reset) %s %C(green)%d%C(reset)' --graph"
