# This is Git's per-user configuration file.
[user]
  name = Stel Abrego
  email = stel@stel.codes
[init]
  defaultBranch = "main"
[merge]
  ff="only"
[pull]
  ff="only"
[core]
  excludesFile="~/.config/git/ignore"
[alias]
  fancy-log = "log --pretty=format:'%C(yellow)%h%C(reset) %C(blue)%an%C(reset) %C(cyan)%cr%C(reset) %s %C(green)%d%C(reset)' --graph"
