# Install sudo if necessary
apt install sudo

# Add user stel if not exists
adduser stel

# Add group to user that enables sudo (defined in /etc/sudoers)
usermod -a -G sudo stel

# If sudo stuff is still wonky, use visudo to edit sudoers file

# (Debian) Add sources.list and preferences to /etc/apt
apt update

