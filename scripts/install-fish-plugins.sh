#! /usr/bin/env bash

# Both the install and update commands download new versions of all the files so this script
# can be run over and over again without negative side effects

fisher install jorgebucaran/fisher
fisher install PatrickF1/fzf.fish

